# === 환경 설정 (.env.config) ===
# 민감하지 않은 설정값들 - 공유 가능

# === Polymarket 설정 ===
# Chain ID (137 = Polygon mainnet, 80002 = Amoy testnet)
POLYMARKET_CHAIN_ID=137

# 마켓 로딩 설정
POLYMARKET_MAX_MARKETS=100
POLYMARKET_USE_EVENTS=false

# WebSocket 설정
POLYMARKET_WS_ENABLED=true

# === 테스트 설정 ===
# 검색 키워드 (빈 문자열이면 카테고리 내 전체 검색)
TEST_SEARCH_QUERY=bitcoin

# 검색 카테고리 필터 (빈 문자열이면 전체, 예: crypto, sports, politics, ai)
TEST_SEARCH_TAG=

# 최소 24시간 볼륨 ($)
TEST_MIN_VOLUME=100000

# 고정 마켓 ID (설정시 검색 대신 직접 사용)
# - Condition ID 입력 (Polymarket URL에서 확인)
# - 비워두면 검색으로 랜덤 선택
TEST_MARKET_ID=

# 테스트 주문 수량
TEST_ORDER_SIZE=10

# 주문 사이즈 타입 (shares = 주식 수, usd = 달러 금액)
TEST_ORDER_SIZE_TYPE=usd

# Split/Merge 테스트 금액 (USDC)
TEST_SPLIT_AMOUNT=1

# === 일반 설정 ===
LOG_LEVEL=INFO
